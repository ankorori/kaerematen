# 一致するまで終われまテン - 設計書

## 概要

お題(ITエンジニア向け)に対して参加者全員がテキストで回答し、全員の回答が一致すれば1問クリア。10回連続で一致させればゲームクリアとなるパーティーゲーム。

## ゲームルール

- お題(例:「好きなステータスコードは？」)に対し、参加者全員がキーボードでテキスト回答する。
- 各お題には30秒の回答タイマーを設ける。時間切れで自動的に締め切られる。
- 回答の一致判定は自動判定(表記ゆれを正規化した上での完全一致)を基本とする。
  - 一致しない場合でも、ホストが「強制一致」ボタンを押すことで手動で一致扱いに上書きできる。
- 毎回、新しいお題を順番に出題する。
  - 全員一致 → 連続記録(streak)を+1し、次のお題へ進む。
  - 不一致 → 連続記録は0にリセットされ、次のお題へ進む(同じお題は再出題しない)。
- 連続記録が10に達した時点でゲームクリア。
- お題を最後まで(順番通りに)出題し切った場合は、最初のお題に戻って繰り返す。

## プレイ形態

- ホストは表示専用の画面(PC)を用意し、Zoom/Discordなどの画面共有機能でその画面を参加者に見せる。
  - 表示専用画面には、お題・タイマー・各参加者の回答一覧(締切後)・現在の連続記録などを表示する。
  - この画面には運営操作ボタンは表示しない。
- 参加者(ホスト自身も含む)は各自のデバイスで「参加用URL」にアクセスする。
  - 参加時にニックネームを入力する(必須)。回答と一緒にホスト画面へ表示するため。
  - ホストは参加用URLにホスト権限付きでアクセスし、自分の回答入力に加えて「強制一致」ボタンなどの運営操作を行える。
- 参加人数の上限は設けない(可変レイアウトで対応)。目安は10人程度。

## スコープ(プロトタイプ)

- アカウント登録機能なし。ルーム作成のみで即プレイ可能。
- データは永続化せず、ルーム単位の揮発データでよい(サーバー再起動やルーム終了で消えてよい)。
- 対象デバイスはPC中心。レスポンシブ対応は必須としない。

## 問題データ

- 事前に用意された問題データはなし。
- 対象がITエンジニアであることを踏まえ、以下のようなカテゴリでお題を作成する:
  - HTTPステータスコード
  - プログラミング言語
  - Git用語・コマンド
  - キーボードショートカット
  - 有名企業・OSS
  - あるあるバグ・エラーメッセージ
- プロトタイプではまず30〜50問程度の叩き台を作成する。
- 出題は順番通りに行い、使い切ったら最初から繰り返す(上記ゲームルール参照)。

## 技術スタック

- フロントエンド/バックエンド: Next.js(React)+ TypeScript
- リアルタイム同期: WebSocket(Socket.io を想定)
  - お題配信、回答締切の同時進行、結果の一斉表示などに使用。
- ホスティング: インターネット上にデプロイする。
  - WebSocketの永続接続が必要なため、Vercelのようなサーバーレス環境単体では制約があり、Railway / Fly.io / Render など WebSocket対応のホスティングを検討する。

## 未確定・実装時に判断する詳細(設計時点でのメモ)

- テキスト正規化ルールの詳細(全角/半角、大文字/小文字、前後の空白、ひらがな/カタカナなど)は実装時に妥当なデフォルトを設定する。
- ゲームクリア後の演出やリスタート導線は実装時に決定する。
- ゲーム進行中に途中参加してきたプレイヤーの扱い(次のお題から合流、など)は実装時に決定する。

## 今後のステップ

1. ITエンジニア向けお題リスト(30〜50問)の叩き台作成
2. 上記設計に基づくアプリケーション実装

## 追加モード: クイズモード

一致するまで終われまテンとは別に、ロビーでホストが選べる「クイズモード」を追加した。

- 老若男女みんなで楽しめる雑学・なぞなぞクイズを全100問(5章、各20問)用意し、ホストが章単位で出題ON/OFFを選べる(`lib/quizQuestions.ts`)。
- 各参加者は個別に正誤判定・採点される(全員一致は不要)。表記ゆれは一致モードと同じ正規化ロジックに加え、問題ごとの別解(`acceptableAnswers`)も許容する。
- 自動判定がおかしい場合は、ホストが問題ごとに参加者の正誤を手動で切り替えられる(スコアも連動して増減)。
- 出題は章内でシャッフルされ、選択した全問(最大100問)を1問ずつ出題し、最後まで到達すると終了してスコアのランキングを表示する(一致モードと異なり、周回はしない)。
